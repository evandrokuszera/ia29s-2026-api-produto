spring:
  application:
    name: api-produto

  datasource:
    url: jdbc:h2:file:./produtos_db
    driverClassName: org.h2.Driver
    username: sa
    password: ""

  h2:
    console:
      enabled: true
      path: /h2-console

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update